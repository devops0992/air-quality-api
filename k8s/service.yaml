apiVersion: v1
kind: Service
metadata:
  name: air-quality-api

spec:
  type: NodePort
  
  selector:
    app: air-quality-api
  
  
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080  